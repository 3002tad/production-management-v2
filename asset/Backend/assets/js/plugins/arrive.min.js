/*! arrive.js v1.4.11 | MIT License */
(function(global, factory) {
    if (typeof module !== 'undefined' && module.exports) {
        module.exports = factory();
    } else {
        global.Arrive = factory();
    }
}(typeof window !== 'undefined' ? window : {}, function() {
    'use strict';

    var Arrive = {
        observeElement: function(selector, callback) {
            var observer = new MutationObserver(function(mutations) {
                mutations.forEach(function(mutation) {
                    if (mutation.type === 'childList') {
                        $(mutation.addedNodes).each(function() {
                            if ($(this).is(selector)) {
                                callback.call(this);
                            }
                        });
                    }
                });
            });

            var config = { childList: true, subtree: true };
            observer.observe(document.body, config);
            return observer;
        },

        on: function(selector, callback) {
            if (typeof jQuery !== 'undefined') {
                return Arrive.observeElement(selector, callback);
            }
        }
    };

    if (typeof jQuery !== 'undefined') {
        jQuery.fn.arrive = function(callback) {
            return Arrive.on(this.selector, callback);
        };
    }

    return Arrive;
}));
